version: '3.8'
services:
  app:
    build: .
    ports:
      - "3100:3000"
    env_file: .env
    restart: unless-stopped
    networks:
      - poc-net
      - supabase_default

networks:
  poc-net:
    external: true
  supabase_default:
    external: true
