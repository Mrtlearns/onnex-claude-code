# v2.0 — ndt-portal/tools/pdf_to_markdown
# Python deps for the batch ASME PDF-to-Markdown converter.
# Tesseract OCR binary must also be installed on the host if OCR is enabled:
#   Ubuntu/Debian:  sudo apt-get install tesseract-ocr
#   macOS:          brew install tesseract
#   Windows:        choco install tesseract  (or download from UB-Mannheim)
#
# pymupdf4llm replaces pdfplumber for column-aware, LLM-grade markdown extraction.

PyMuPDF>=1.24.0
pymupdf4llm>=1.0.0
Pillow>=10.0.0
pytesseract>=0.3.10
pyyaml>=6.0
